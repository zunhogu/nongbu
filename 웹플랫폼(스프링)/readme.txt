yis-project

압축 파일을 해제 후 Eclips, IntelliJ 등과 같은 IDE를 통해 build.gradle을 열어준다.
빌드가 진행된 후 프로그램을 실행한다.

IDE를 사용하지 않을 경우 압축 해제 후 gradlew build를 통해 build를 실시한다.
그후 build/libs에 있는 jar파일을 실행시킨다.


id : user , pw : 1234 , name: 가나다 로 되어있는 사용자로 로그인이 가능하다.
혹은 원하는 경우 직접 회원가입을 통해 사용자를 생성할 수 있다.

프로젝트 파일은 용량이 커서 메일로 보내겠습니다.